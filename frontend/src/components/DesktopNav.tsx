import { Button } from './ui/button';

const DesktopNav = () => {
    return (
        <Button variant="ghost" className="font-bold">
            Login
        </Button>
    );
};

export default DesktopNav;
